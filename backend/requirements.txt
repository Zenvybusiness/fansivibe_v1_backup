# Test/development environment (21.2-B): pinned production runtime
# dependencies plus the test runner. Production Docker image installs
# ONLY requirements-prod.txt (pytest never ships to prod).
-r requirements-prod.txt
pytest==9.1.1
