# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# 21.2-A — secrets / local-only files (never commit real values)
.env
.env.*
!.env.example
key.properties
*.jks
*.keystore
local.properties

# 21.2-A — local virtualenvs and logs
.venv/
*.log
