import 'package:flutter/material.dart';

/// Centralized color tokens for Fansivibe design system.
///
/// Follows a premium, dark luxury visual direction.
abstract final class FansivibeColors {
  /// Near-black background color.
  static const Color background = Color(0xFF0A0A0A);

  /// Dark charcoal surface color for cards and panels.
  static const Color surface = Color(0xFF1E1E1E);

  /// Warm gold accent color.
  static const Color accentGold = Color(0xFFC5A059);

  /// Soft off-white primary text color.
  static const Color textPrimary = Color(0xFFE5E5E5);

  /// Muted secondary text color.
  static const Color textSecondary = Color(0xFF9E9E9E);
}
